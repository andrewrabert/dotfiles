#!/usr/bin/env sh
SCRATCH_DIR=~/.local/share/claude-scratch
mkdir -p "${SCRATCH_DIR}"
cd "${SCRATCH_DIR}" || exit 1
if command -v claude-bwrap > /dev/null; then
    exec claude-bwrap "$@"
else
    exec claude "$@"
fi
